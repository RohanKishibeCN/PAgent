# =============================================================================
# PA Agent 环境变量配置
# 将此文件复制为 .env 并按需修改：
#   cp .env.example .env
#
# .env 文件已在 .gitignore 中，不会提交到 Git。
# 敏感信息优先使用 .env，而非写在 settings.json 中。
# =============================================================================

# ── AI 模型 API Key ──────────────────────────────────────────────────────────
# DeepSeek / OpenAI 等大模型的 API Key
# AI_API_KEY=sk-your-key-here

# ── 交易所 API Key（自动交易用）──────────────────────────────────────────────
# 在 OKX / Binance 等交易所后台创建 API Key。
# ⚠️ 安全规则：
#   1. 只勾选 "交易" + "读取" 权限
#   2. NEVER 勾选 "提现" 权限
#   3. 绑定 IP 白名单到 VPS IP
# EXCHANGE_API_KEY=your-exchange-api-key
# EXCHANGE_SECRET=your-exchange-secret
# EXCHANGE_PASSWORD=your-exchange-password  # OKX 需要 passphrase
